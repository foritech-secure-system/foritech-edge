# Foritech Edge

Installer for Foritech Edge security agent.

Install:

curl -fsSL https://forisec.eu/install-edge.sh | bash

This installs the Foritech Edge CLI on industrial gateways (IoT2050, Linux edge nodes).
